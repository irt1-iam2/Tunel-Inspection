/* Descarga el archivo desde https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js y colócalo en el directorio raíz del proyecto. El archivo es grande (~1MB), por lo que no se incluye aquí directamente. */